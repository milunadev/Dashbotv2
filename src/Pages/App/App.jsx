import { Provider } from "../../Context";
import {AppUI} from "./AppUI"

function App(){
    return(
        <Provider>
            <AppUI>

            </AppUI>
        </Provider>
    )
}

export default App;